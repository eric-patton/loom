// TODO(M4): define OpaqueNode — holds verbatim source range for constructs
// the model does not represent (closures, conditionals, .map().toList()).
// Mutations forbidden; content round-trips byte-identically.
